# Dependencias directas usadas para generar los modelos publicados.
# PyTorch se resuelve a través de stable-baselines3 para permitir builds CPU/CUDA.
gymnasium==1.2.3
matplotlib>=3.8,<4
numpy==2.2.6
pygame>=2.5,<3
stable-baselines3[extra]==2.8.0
